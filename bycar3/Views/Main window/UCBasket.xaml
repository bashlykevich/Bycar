<UserControl x:Class="bycar3.Views.Main_window.UCBasket"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Style="{DynamicResource ResourceKey=ShatemUserControl}"
             mc:Ignorable="d"
             xmlns:my="http://schemas.microsoft.com/wpf/2008/toolkit"
             xmlns:l="clr-namespace:UIControls;assembly=UIControls"
             d:DesignHeight="622" d:DesignWidth="1180">
    <Grid
        Name="grMain"
        Style="{DynamicResource ResourceKey=ShatemGrid}"
        Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*" />
            <ColumnDefinition Width="0.4*" />
            <ColumnDefinition Width="0.3*" />
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>